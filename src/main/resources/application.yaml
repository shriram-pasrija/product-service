server:
  port: 8080
  servlet:
    context-path: /api

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/productservice
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa.show-sql: true